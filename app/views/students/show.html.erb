<div class="row">
  <div class="col-md-8">

    <h1>Student Name: <%= @student.name %></h1>
    <h1>Student Grade: <%= @student.grade %></h1>

    <div class="col-md-4">
   <%= link_to "Edit Student", edit_student_path(@student), class: 'btn btn-success' %>
   <%= link_to "Delete Student", @student, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this student?'} %>
   </div>

  </div>
</div>
